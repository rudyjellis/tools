{
  "name": "claude-code-sandbox",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Cloudflare Sandbox for running Claude Code in isolated containers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/sandbox": "0.6.10",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.50.0"
  }
}
